apiVersion: v1
kind: Service
metadata:
  name: stirling-pdf
  namespace: apps
  labels:
    app: stirling-pdf
spec:
  selector:
    app: stirling-pdf
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
